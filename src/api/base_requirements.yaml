# base requirements copied from https://github.com/openproblems-bio/core/blob/main/base_images/src/python/config.vsh.yaml
setup:
  - type: apt
    packages:
      - procps
      - git
  - type: python
    packages:
      - anndata~=0.10.0
      - scanpy~=1.10.0
      - pyyaml
      - requests
      - jsonschema
    github:
      - openproblems-bio/core#subdirectory=packages/python/openproblems
