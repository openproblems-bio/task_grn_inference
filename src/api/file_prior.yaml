type: file
example: resources/grn-benchmark/prior_data.h5ad
info:
  label: prior data
  summary: "Prior data used for grn benchmark"
  file_type: h5ad
  slots:
    uns:
      - type: list
        name: tf_list
        description: List of known tfs obtained from https://resources.aertslab.org/cistarget. 
        required: true